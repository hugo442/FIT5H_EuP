<p id="notice"><%= notice %></p>

<h1>Gruppen:</h1>
<br><br>

<div class="row">
  <div class="small-2 large-4 columns"><%= link_to 'Neue Gruppe', new_group_path, class: "button small primary" %></div>
  <div class="small-4 large-4 columns"></div>
  <div class="small-6 large-4 columns"></div>
</div>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Note</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.name %></td>
        <td><%= group.note %></td>
        <td>
          <%= link_to 'Details', group, class: "button small warning" %>
          <%= link_to 'Edit', edit_group_path(group), class: "button small secondary" %>
          <%= link_to 'Löschen', group, method: :delete, data: { confirm: 'Are you sure?' }, class: "button small alert" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

